{
    "_comment": "CASE 2C -- PARTIAL PARTICIPATION, MILP (single-path) leg. Same shared-NIC topology, same active set [0, 2, 3, 4, 5, 6], same clock and same objective as mini_2gpu_1nic_alltoall_partial_2C.json, solved with the chunk-level MILP (formulation 1) at num_chunks 1: each (src, dst) pair owns exactly ONE indivisible 1 GB chunk that traverses exactly ONE path end to end. The solver still picks the BEST single-path assignment, so LP-minus-MILP is a LOWER bound on what per-pair multipath is worth. This is the largest MILP of the partial set -- 30 chunks on 16 nodes -- so expect it to be the slow one.",
    "_known_limitation": "KNOWN LIMITATION, ACCEPTED -- MILP timing on a non-uniform-bandwidth topology at a sub-chunk epoch. This topology mixes 100Gbps host ports with 50Gbps leaf-spine uplinks, and this file's pinned 0.08 s epoch is SHORTER than one chunk on the 50Gbps link (6.25 GB/s x 0.08 s = 0.5 GB against a 1 GB chunk), so get_beta_num_back returns beta = 1 there. In that regime the MILP's cut-through accounting under-charges TIME: allgather.py pays the store-and-forward cost (1 epoch + beta) ONCE at the first GPU->switch hop and charges every later fabric hop only propagation delay (see the branches at allgather.py switch->switch and the switch->GPU buffer credit), so the delivery epoch it credits is 1 + beta(FIRST hop) instead of the correct 1 + max(beta over the legs used) = ceil(chunk / min bandwidth along the path). CONSEQUENCE: in a MILP schedule from this file, 3-Epochs_Required, 4-Collective_Finish_Time and 5-Algo_Bandwidth are OPTIMISTIC and must not be quoted -- the ROUTING (7-Flows and 8-Chunk paths) is what the file is for, and real timing comes from simulating that routing. Measured on case 1A at this epoch: the MILP reports 0.08 s while every 50Gbps leg on its own chosen path needs 0.16 s to clock the chunk out; the identical run with switch_pipeline false (which does charge the -1 and beta) reports 0.48 s. The LP is NOT affected: continuous flow can be split across epochs, so it has no beta machinery and its per-epoch row (capacity x epoch_duration) is exact -- which is why the fine epoch is kept here rather than coarsened, since it is what makes the LP's makespan exact instead of rounded up. mini_2gpu_2nic (case 3A) is structurally immune: its links are uniform 100Gbps, so one chunk always fits one epoch and beta is 0 everywhere.",
    "_participation": "active GPUs [0, 2, 3, 4, 5, 6]; passive GPUs [1, 7]. Node map: 0/1 = srv0.gpu0/gpu1, 2/3 = srv1, 4/5 = srv2, 6/7 = srv3; 8-11 = the rc's; 12/13 = leaf0/leaf1; 14/15 = spine0/spine1. srv0+srv1 on leaf0, srv2+srv3 on leaf1, so participation is 3-and-3 across the leaves and 1/2/2/1 across the hosts. Passive GPUs still relay; num_chunks 1 = one chunk per (src, dst) PAIR, and the chunk axis is widened by the ACTIVE count, giving 30 chunks of 1 GB.",
    "_vs_lp": "Differences from mini_2gpu_1nic_alltoall_partial_2C.json, and only these: formulation 1 (MILP) not 2 (LP); symmetry false not true; num_epochs 14 not 16; distinct log_file and schedule_output_file. Held fixed: topo_file, passive_node_indices, chunk_size 1, collective 2, num_chunks 1, switch_copy false, epoch_type 3 / epoch_duration 0.08 (pinned, identical in both), switch_pipeline true, solution_method 1, time_limit 5. ALSO DIFFERENT: objective_type 2 (TOTAL_DEMAND) here against the LP's 6 (LEXICOGRAPHIC) -- so the difference is integrality PLUS objective, not integrality alone; it is still not granularity. See _objective_note for exactly what that trades.",
    "_objective_note": "OBJECTIVE: TOTAL_DEMAND (objective_type 2), NOT the lexicographic 6 the LP partner uses. WHAT IT IS: allgather.py::objective_formulation builds -100 * sum_k max(sum_(s,d,c) total_demand_sat[s][d][c][k] - (Demand - 1), 0). The inner term is 1 exactly when EVERY demand is already satisfied by the end of epoch k and 0 otherwise, so the objective pays 100 per epoch of the horizon that the collective has already finished in -- i.e. it MINIMIZES THE COMPLETION EPOCH directly. That is the point of using it here: '3-Epochs_Required' becomes the quantity the solver is actually optimizing, so a reported makespan can be compared against the analytic bound as a real optimality claim rather than as an artifact of where a latency-sum objective happened to leave the last straggler. WHAT IT GIVES UP -- read this before judging the routing: objective_type 6 carried two lower-priority tiers that 2 does not have. Tier 2 penalized HOST RELAY (volume a GPU forwards on behalf of another GPU) and tier 3 penalized SWITCH-CHAIN LENGTH (flow entering a switch: 0 for a direct link, 1 for gpu->switch->gpu, 3 for a leaf->spine->leaf detour). Under objective 2 the solver is INDIFFERENT among all schedules that finish in the same epoch, so expect gratuitous relays and spine detours that carry no cost -- the schedule stays correct and makespan-optimal, but it is no longer the tidiest routing that achieves that makespan, and a relay or a long chain in the output is no longer evidence that it was needed. MIDDLE GROUND if that matters: objective_type 5 (TOTAL_DEMAND_MIN_SWITCH_HOPS) is this same completion reward plus a GAMMA=1e-4 per-unit penalty on switch->switch flow, small enough never to trade completion time for hops, which restores a shortest-chain preference (but still no relay tier). COST: the objective adds two continuous aux vars and one max_ general constraint per epoch, which is negligible next to the (source, link, chunk, epoch) flow variables. COMPARABILITY CAVEAT: the LP partner still uses objective_type 6, so the LP-vs-MILP difference is no longer integrality ALONE -- it is integrality plus objective. For the makespan that is fine (both are measured against the same analytic lower bound, which neither objective can beat); for a ROUTING diff, remember the LP is tie-broken toward fewer relays and shorter switch chains and this MILP is not.",
    "_why_symmetry_off": "Orbit averaging is free on the LP (convex feasible set, so the symmetric average of an optimum is an optimum) but a REAL restriction on the integral problem, where an equal count across an orbit need not exist at the integral optimum. Cutting off that optimum is the measurement error this file exists to avoid.",
    "_expected": "WHAT THIS FILE IS FOR IS THE ROUTING, NOT THE CLOCK -- its reported makespan is optimistic at this epoch (see _known_limitation), so take timing from simulation. For reference: the cut needs 9 chunks per direction and a 50Gbps uplink clocks one chunk out in 0.16 s = 2 epochs, so 2 uplinks pass a chunk each 2 epochs and the hand-computed single-path answer is about 9 x 2 / 2 = 9 epochs = 0.72 s -- i.e. roughly a TIE with the LP on volume, because the cut is rate-bound rather than fragmentation-bound at this size. The shared NICs stay slack: srv1 and srv2 pass 8 chunks at 1 per epoch = 8 epochs. WHAT TO COMPARE: diff the '30-Chunk paths' sections -- every MILP demand lists ONE path, the LP's 18 cross-leaf demands should be fanned across both spines -- and check that both keep each middle host's intra-host pair (2<->3, 4<->5) on the NVLink bridge rather than out through the shared NIC. This is the largest MILP of the partial set (30 chunks on 16 nodes over a 14-epoch horizon) and will be the slow one. MEASURED UNDER THE OLD OBJECTIVE (objective_type 6, 24 s solve): 11 epochs = 0.88 s, TWO above the 9-epoch analytic bound, while its own routing needed only 0.80 s at the aggregate rate bound -- i.e. the 2 epochs were an artifact of a latency-sum objective inside a generous horizon, not a property of single-path routing. That is exactly why this leg now uses objective_type 2, which minimizes the completion epoch directly (see _objective_note). EXPECT 9 or 10 epochs on a re-run; if it still comes back at 11, the single-path optimum really is 11 and the integrality gap against the LP's 9 is real. This number has NOT been re-measured since the objective change.",
    "_epoch_note": "epoch_type 3 (USER_INPUT) with epoch_duration PINNED to 0.08 s = 1 GB / 12.5 GB/s, one 100Gbps host port. Deliberately NOT the SLOWEST_LINK value (0.16 s, the 50Gbps uplink) that the full-participation files on this topology use: at 0.16 s the LP's answer is rounded up to the epoch grid, and pinning 0.08 s is what makes the LP's makespan land exactly on its true bound. The price is paid by the MILP leg only, and it is a KNOWN, ACCEPTED inaccuracy in its reported time -- see _known_limitation. Both legs share this clock so the routing comparison stays controlled.",
    "_horizon_note": "num_epochs is the MILP's time horizon and it costs -- one integer flow variable per (source, link, chunk, epoch), and with 30 chunks on 16 nodes that product is the whole model size, so 10 against a 5-epoch bound is deliberately only 2x. If the solve comes back INFEASIBLE rather than merely slow, raise this first; if it comes back slow, raise time_limit or drop to solution_method 2 instead. With objective_type 2 the horizon no longer DISTORTS the answer, only the model size: the reward is paid per epoch in which the whole collective is already complete, so a larger num_epochs just widens the reward range and the solver still drives the completion epoch down. Contrast objective_type 6, under which the reported makespan was only an upper bound (its tier 1 minimizes the SUM of completion epochs, not the max, so a straggler could drift late inside a generous horizon) -- see _objective_note.",
    "_why_flat_only": "hierarchical is false and cannot be true on this topology for either formulation: the host's single external link is the NIC, owned by `rc` rather than by a GPU, so DslTopology._build_cell will not accept these hosts as cells.",
    "_topology_note": "The topology comes from the .topo DSL file; topo_file SELECTS it, so TECCLSolver.get_topology builds a DslTopology and `name` is only the free-form label for output filenames and the Gurobi log. The path is relative to the repo root.",
    "TopologyParams": {
        "topo_file": "teccl/topologies/topology-dsl-frontend/examples/mini_2gpu_1nic.topo",
        "name": "Mini2GPU1NIC",
        "chassis": 1,
        "chunk_size": 1,
        "passive_node_indices": [
            1,
            7
        ]
    },
    "GurobiParams": {
        "time_limit": 5,
        "feasibility_tol": 1e-06,
        "optimality_tol": 1e-06,
        "output_flag": 1,
        "log_file": "_mini_2g1n_a2a_2C_milp",
        "log_to_console": 0,
        "method": -1,
        "crossover": -1
    },
    "InstanceParams": {
        "collective": 2,
        "formulation": 1,
        "switch_copy": false,
        "num_chunks": 1,
        "epoch_type": 3,
        "epoch_duration": 0.08,
        "epoch_multiplier": 1,
        "num_epochs": 14,
        "alpha_threshold": 0.1,
        "switch_pipeline": true,
        "symmetry": false,
        "objective_type": 2,
        "solution_method": 1,
        "hierarchical": false,
        "schedule_output_file": "Schedules/mini_2gpu_1nic_alltoall_partial_2C_milp.json"
    }
}