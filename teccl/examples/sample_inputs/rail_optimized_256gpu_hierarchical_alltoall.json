{
    "_comment": "Hierarchical AllToAll on the SINGLE-PLANE rail-optimized 256-GPU cluster (300 fine nodes -> 44 coarse: 32 host cells + 8 leaves + 4 spines). Topology comes from teccl/topologies/topology-dsl-frontend/examples/rail_optimized_256gpu.topo; the path is relative to the repo root, i.e. to the directory `teccl solve` runs from. This is the NON-BLOCKING counterpart of rail_optimized_256gpu_dual_plane_hierarchical_alltoall.json (same 32x8 GPUs, same 8 leaves / 4 spines, same 1800 GB/s NVSwitch inside a host); the two differ only in the fabric cut -- one 400 Gbps rail per GPU into a non-blocking spine tier here, versus 2x400 Gbps into a 2:1 tapered spine tier there.",

    "_expected": "At chunk_size = 1 GB per (src,dst) GPU pair, each GPU ships 248 GB across the host boundary (255 peers - 7 host-mates that stay on the NVSwitch). Binding cut is the GPU RAIL: 248 GB / 50 GB/s = 4.96 s, i.e. 400 GB/s of host egress against 1984 GB per host. The spine cut is SLACK, as a non-blocking fabric should be: each leaf carries 32 x 217 GB = 6944 GB of up-traffic (217 = 248 - the 31 same-rail peers that turn around on the leaf) against 4 x 400 = 1600 GB/s of uplink = 4.34 s. So T* >= 4.96 s = 4 pinned epochs, and the leaf->spine tier should never be the reason a solution slips.",

    "_epoch_note": "epoch_duration is PINNED to 1.24 s at the ROOT level only (list form: [USER_INPUT, FASTEST_LINK] / [1.24, -1]; depths past the end of a list take its last entry, so every level below the coarse LP keeps deriving its own epoch). 1.24 s is chosen so the 4.96 s bound lands on an integer epoch count (4) with nothing lost to the final ceiling. FASTEST_LINK at the root would size the epoch off the coarse leaf-spine link, 2048 GB (a host's whole send buffer, the coarse chunk) / 400 GB/s = 5.12 s -- a single epoch for the entire collective, which quantizes the answer into uselessness. The dual-plane sibling pins the SAME 1.24 s, so the two runs are directly comparable epoch for epoch. The pin is legal because the root is solved by a real formulation (the LP answers a shorter epoch with more epochs); the >= fastest-link floor in hierarchy/solve.level_epoch_duration only guards the closed-form levels, which is why they are left on FASTEST_LINK.",

    "TopologyParams": {
        "topo_file": "teccl/topologies/topology-dsl-frontend/examples/rail_optimized_256gpu.topo",
        "name": "RailOptimizedSinglePlane",
        "chassis": 1,
        "chunk_size": 1
    },
    "GurobiParams": {
        "time_limit": 5,
        "feasibility_tol": 1e-5,
        "optimality_tol": 1e-5,
        "output_flag": 1,
        "log_file": "_rail_sp_alltoall",
        "log_to_console": 0,
        "method": -1,
        "crossover": -1
    },
    "InstanceParams": {
        "collective": 2,
        "formulation": 2,
        "switch_copy": false,
        "num_chunks": 1,
        "epoch_type": [3, 1],
        "epoch_duration": [1.24, -1],
        "epoch_multiplier": 1,
        "num_epochs": -1,
        "alpha_threshold": 0.1,
        "switch_pipeline": true,
        "symmetry": true,
        "objective_type": 6,
        "solution_method": 1,
        "hierarchical": true,
        "hierarchy_prefix": "rail_sp_alltoall_epochs",
        "hierarchy_side_outputs": true,
        "schedule_output_file": "Schedules/rail_optimized_256gpu_alltoall_flat.json"
    }
}
